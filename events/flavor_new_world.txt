namespace = flavor_nworld

country_event = {
	id = flavor_nworld.1
	title = "flavor_nworld.1.t"
	desc = "flavor_nworld.1.d"
	picture = DIPLOMACY_eventPicture
  fire_only_once = yes
	trigger = {
		OR = {
			religion = celt
			religion = norse_pagan_reformed
		}
		980 = {
			has_discovered = ROOT
		}
	}
	mean_time_to_happen = {
		months = 1
	}

	option = {		# help them
		name = "flavor_nworld.1.a"
    2573 = {
			if = {
				limit = {
						is_city	= no
						is_colony	= no
				}
				change_culture = Vinlandic
				change_religion = norse_pagan_reformed
				cede_province = ROOT
			}
    }
    981 = {
			if = {
				limit = {
						is_city	= no
						is_colony	= no
				}
				change_culture = Vinlandic
				change_religion = norse_pagan_reformed
				cede_province = ROOT
			}
    }
    980 = {
			if = {
				limit = {
						is_city	= no
						is_colony	= no
				}
				change_culture = Vinlandic
				change_religion = norse_pagan_reformed
				cede_province = ROOT
			}
    }
    declare_war = INN
	}

  option = {		# Vinlandic independant
		name = "flavor_nworld.1.b"
    	2573 = {
				if = {
					limit = {
							is_city	= no
							is_colony	= no
					}
	      change_culture = Vinlandic
	      change_religion = norse_pagan_reformed
	      cede_province = VIN
			}
    }
    981 = {
				if = {
					limit = {
							is_city	= no
							is_colony	= no
					}
				change_culture = Vinlandic
				change_religion = norse_pagan_reformed
				cede_province = VIN
			}
    }
    980 = {
			if = {
				limit = {
						is_city	= no
						is_colony	= no
				}
			change_culture = Vinlandic
			change_religion = norse_pagan_reformed
			cede_province = VIN
			}
    }
    INN = {
      declare_war = VIN
    }
	}
  option = {		# Vinlandic independant
		name = "flavor_nworld.1.c"
    trigger = {
      religion = celt
    }
    2573 = {
			if = {
				limit = {
						is_city	= no
						is_colony	= no
				}
			change_culture = Vinlandic
			change_religion = norse_pagan_reformed
			cede_province = VIN
			}
    }
    981 = {
			if = {
				limit = {
						is_city	= no
						is_colony	= no
				}
			change_culture = Vinlandic
			change_religion = norse_pagan_reformed
			cede_province = VIN
			}
    }
    980 = {
			if = {
				limit = {
						is_city	= no
						is_colony	= no
				}
			change_culture = Vinlandic
			change_religion = norse_pagan_reformed
			cede_province = VIN
			}
    }
    declare_war = VIN
	}
}
